
image_xscale = lerp(image_xscale, 1, 0.15);
image_yscale = lerp(image_yscale, 1, 0.15);

if (pizza_tocou)
{
    // A pizza tocou no forno agora (uma única vez)

	pizza_tocou = false;
    // colocar aqui o que o forno faz:
    // criar partículas
    // tocar som
    // ativar alguma coisa
}


